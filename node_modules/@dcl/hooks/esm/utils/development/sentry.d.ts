import * as Sentry from "@sentry/browser";
type SentryTracker = (sentry: typeof Sentry) => void;
declare const sentry: (tracker: SentryTracker) => void;
export type { SentryTracker };
export { sentry };
